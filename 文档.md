
flutter clean
flutter pub get

修改摘要：
1. android/gradle/wrapper/gradle-wrapper.properties:5
将 Gradle 发行版下载地址改为腾讯云镜像：
- distributionUrl=https\://services.gradle.org/distributions/gradle-8.14-all.zip
+ distributionUrl=https\://mirrors.cloud.tencent.com/gradle/gradle-8.14-all.zip

2. android/settings.gradle.kts:13-17
添加腾讯云 Maven 镜像作为优先仓库：
repositories {
maven { url = uri("https://mirrors.cloud.tencent.com/nexus/repository/maven-public/") }
google()
mavenCentral()
gradlePluginPortal()
}

3. android/build.gradle.kts:1-6
添加腾讯云 Maven 镜像：
allprojects {
repositories {
maven { url = uri("https://mirrors.cloud.tencent.com/nexus/repository/maven-public/") }
google()
mavenCentral()
}
}

# SkinTrade Pro - Flutter App 启动文档
# 开发配置
1. 配置环境

# flutter_app 启动步骤
> adb connect 127.0.0.1:7555    链接设备
> flutter run

## 文件结构
├── lib
    ├── api	            数据请求和定义
    ├── assets	        静态文件资源（图片）
    ├── common	        一些工具类，如通用方法类、网络接口类、保存全局变量的静态类等
    ├── components	    静态文件
    ├── controllers	    业务层抽离文件
    ├── l10n	        国际化相关的类都在此目录下
    ├── pages	        APP内封装的一些Widget组件都在该目录下
    ├── routes	        存放所有路由页面类
    ├── states	        保存APP中需要跨组件共享的状态类

## 代码规范
* 页面组件拆分（将不同部分拆分为独立Widget）：使用_前缀命名表示私有,如比 _UserHeader。我们统一放在当前文件的widgets目录下
* 文件命名：小写字母 + 下划线（snake_case），如 login_page.dart。
* 变量命名：小驼峰（lowerCamelCase），如 userName。
* 常量命名：全大写 + 下划线（UPPER_SNAKE_CASE），如 MAX_COUNT。
* 函数与方法：小驼峰，动词开头（如 fetchUserData）
* 类与枚举：
        类命名：大驼峰（UpperCamelCase），如 UserModel。
        枚举命名：大驼峰，值全大写： 如 enum Status { LOADING, SUCCESS, ERROR }
* 组件命名：
        基础组件：CustomXxx（如 CustomButton）。
        业务组件：模块名Xxx（如 AuthLoginButton）。
* 布局与样式：
        避免深层嵌套(使用 Column/Row + Expanded 或 Wrap 替代多层嵌套)
        样式提取:
                文本样式：定义在 themes/theme_text.dart。
                颜色：定义在 themes/theme_color.dart。
> 图片命名：全小写 + 下划线分隔（snake_case.png）

## 技术选型
* 路由(GetX)+底部导航栏[https://gitcode.com/gh_mirrors/ge/getx/blob/master/documentation/zh_CN/route_management.md]
* 状态管理(GetX)[https://gitcode.com/gh_mirrors/ge/getx/blob/master/documentation/zh_CN/state_management.md]
* 多语言[https://gitcode.com/gh_mirrors/ge/getx?utm_source=csdn_github_accelerator&isLogin=1&from_link=a98b01a05edf7fcaec98eb1f6c654053#%E5%AE%9E%E7%94%A8%E5%B7%A5%E5%85%B7]
   > 使用：Text('app.tabbar.home'.tr)
* 多主题
    ```
        // 文本使用
        Text('Hello World', style: TextStyle(color: Theme.of(context).textTheme.bodyLarge!.color));

        // 按钮使用
        ElevatedButton(
        style: ElevatedButton.styleFrom(
            backgroundColor: Theme.of(context).colorScheme.primary,
        ),
        onPressed: () {},
        child: Text('CTA Button'),
        );

        // 自定义扩展色使用
        Text('Success message', style: TextStyle(color: Theme.of(context).extension<ColorExtension>()!.success));
    ```
* 网络请求封装(Dio)
    ```
        Future<BaseHttpResponse> getCurrencyList() async {
            final response = await http.get('api/currency/exchange/rate/list.do');
            return BaseHttpResponse.fromJson(response.data,(data) => data);
        }
        
        final apiService = ApiSystemServer();
        final response = await apiService.getCurrencyList();
    ```
* 本地存储(get_storage)
    ```
    // 需要在main函数中init一下
    main() async {
        await GetStorage.init();
        runApp(App());
    }
    ```
    // 使用
    final GetStorage cookie = GetStorage();
    + 存 cookie.write(key, value);
    + 取 cookie.read(key);
    + 删 cookie.remove(key);
    + 清空所有 cookie.erase();
* echarts图表(flutter_echarts)
* 个推方案(getui-flutter-plugin)
* 热更新方案(Shorebird)
* icon图标(https://fluttericon.cn/)
* 汇率切换
* 日志打印(logger):  
  > logger.d('这是一条debug信息');
  > logger.i('这是一条info信息');
  > logger.e('这是一条error信息');

